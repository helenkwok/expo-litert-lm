# CLiteRTLM.xcframework binary is gitignored — it is a large platform binary
# fetched at install time via scripts/fetch-litert-lm.sh (pinned by SHA-256
# to a specific google-ai-edge/LiteRT-LM release).
#
# Run `scripts/fetch-litert-lm.sh` from the repo root to populate.
*.xcframework
